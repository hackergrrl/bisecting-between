{
  "name": "between2",
  "description": "produces a unique id between two other given ids",
  "version": "0.0.0",
  "repository": {
    "url": "git://github.com/noffle/between2.git"
  },
  "main": "index.js",
  "scripts": {
    "test": "tape test/*.js"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "*"
  }
}
