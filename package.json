{
  "name": "bisecting-between",
  "description": "Produces a unique value that sorts between two other given values.",
  "version": "0.0.7",
  "repository": {
    "url": "git://github.com/noffle/bisecting-between.git"
  },
  "main": "index.js",
  "scripts": {
    "test": "tape test/*.js"
  },
  "dependencies": {
    "bisecting-numbers": "^0.1.1"
  },
  "devDependencies": {
    "tape": "*"
  }
}
